import React from "react";
import ReactDOM from "react-dom/client";
import PIPlannerPrototype from "./PIPlannerPrototype.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PIPlannerPrototype />
  </React.StrictMode>
);
